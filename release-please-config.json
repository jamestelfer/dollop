{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "release-type": "simple",
  "pull-request-header": "Release prepared by release-please. Merge to tag and run the release pipeline.",
  "extra-files": ["flake.nix"],
  "packages": {
    ".": {}
  }
}
